name: Lint checks

on:
  pull_request:
    branches:
    - main
    paths:
    - 'tox.ini'
    - 'pdns_auth_tsigkey.py'
    - 'pdns_auth_zone.py'
    - 'workflow-support/*.sh'
    - '.github/workflows/lint.yml'
    - '.github/actions/lint/action.yml'

jobs:
  image_details:
    runs-on: ubuntu-20.04
    outputs:
      image_base: ${{ steps.details.outputs.image_registry }}/${{ steps.details.outputs.registry_account }}/${{ steps.details.outputs.repo_name }}
    steps:
    - id: details
      uses: kpfleming/composite-actions/image-details@main
  lint:
    runs-on: ubuntu-20.04
    needs:
    - image_details
    container:
      image: ${{ needs.image_details.outputs.image_base }}:pdns-master-main
    steps:
    - uses: kpfleming/composite-actions/lint-tox@main
